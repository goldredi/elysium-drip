# EMOJIREAN — полная документация проекта
> #няшныйдвиж · powered by driptech ⁂ · токен — это ключ · слово — это дверь

---

## 1. ЧТО ЭТО

Emojirean — браузерная система шифрования текста в эмоджи с визуальным оформлением.
Одна HTML-страница, без зависимостей, без сервера. Работает полностью в браузере.

**Главная идея:** получатель видит красивую фигуру из эмоджи и ASCII символов.
Без токена это выглядит как арт. С токеном — читается как обычный текст.

---

## 2. АРХИТЕКТУРА КОДИРОВКИ (v3, bigram engine)

### 2.1 Алфавит

```
Пул эмоджи: ~380 уникальных emoji
RU:     33 буквы — каждая получает 4-8 омофонов (гласные больше, редкие меньше)
EN:     26 букв — 3 омофона каждая
DIGITS: 0-9 — 2 омофона
SPACE:  1 выделенный эмоджи
BIGRAMS: 80 пар — 3 омофона каждая (НА, СТ, ЧТ, РО, ЕН...)
NULLS:  12 эмоджи — визуальный шум, не несут смысла
```

**Омофоны = вариативность.** Одна буква → случайный из 4-8 эмоджи → одно слово
каждый раз выглядит иначе, но декодируется одинаково.

### 2.2 Биграммный движок

Алгоритм кодирования — жадный longest-match:

```
i = 0
while i < text:
    если text[i:i+2] есть в bigrams:
        → один эмоджи для двух букв, i += 2
    иначе если text[i] есть в letters:
        → один эмоджи для одной буквы, i += 1
    иначе:
        пропустить символ (# . , и т.д.)
```

**Сжатие биграммами:** ~22% для среднего русского текста.
80 символов → ~76 эмоджи (с учётом пробелов-эмоджи).
Это предел для буквенного подхода без словаря.

### 2.3 Токен

Токен = base64(JSON) со всей таблицей маппинга.

```json
{
  "id": "lang_1234567890",
  "name": "MYTOKEN",
  "v": 3,
  "mode": "ru",          // "ru" | "en" | "both"
  "type": "master",      // "master" | "reader"
  "letters": {
    "А": ["🌑","🦊","💎","🔮","🧿","🐺","🌸","🍀"],
    "Б": ["🌺","🌻","🌹","💫"],
    ...
  },
  "bigrams": {
    "НА": ["⭐","🔥","❄️"],
    "СТ": ["🌊","💥","⚡"],
    ...
  },
  "spaceEmoji": "🦋",    // пробел между словами
  "spaceSep": "·",        // разделитель внутри строк (в artistic mode)
  "nullEmojis": ["🎭","🎪",...], // шум — 12 штук
  "decoStyle": 2,         // 0=WING 1=BOX 2=MATH 3=RUNE 4=BRAILLE
  "nulls для anchor": nullEmojis[0..4] → форма сообщения
  "ttl": null,            // или число (N сообщений) или ISO дата
  "created": "2026-03-13T..."
}
```

**Reader токен** — только `rev_mapping: {emoji: char}`, без прямого словаря.
Получатель может декодировать, но не может зашифровать и не видит алфавит.

### 2.4 Пять стилей дeco ASCII

Каждый токен рождается с одним случайным стилем. Стиль хранится в `decoStyle`.

```
0 = WING  (wingdings): ✮ ☠ ☂ ☁ ☀ ☼ ☽ ☾ ☣ ❀ ї ☄ ✯ ♣ ♧ ◔ ◕ ♬ ✽ ♫ ♪ ° ❃ ❉ ✣ ⁂ ✦ * .
1 = BOX   (box draw):  ░ ▒ ▓ │ ┤ ╔ ╗ ╚ ╝ ─ ═ ╠ ╣ ╦ ╩ ╬ ▐ ▌ ▀ ▄ ▪ ▫
2 = MATH  (math/logic): ∴ ∵ ∞ ≡ ∇ ⋮ ∑ ∏ ∂ ∫ √ ∆ ≈ ∃ ∀ ∈ ⊕ ⊗ ⊙
3 = RUNE  (elder futhark): ᚠ ᚢ ᚦ ᚨ ᚱ ᚲ ᚷ ᚹ ᚺ ᚾ ᛁ ᛃ ᛇ ᛈ ᛉ ᛊ ᛏ ᛒ ᛖ ᛗ ᛚ ᛜ
4 = BRAILLE: ⠿ ⠯ ⠷ ⠾ ⠸ ⠼ ⠻ ⠽ ⠱ ⠩ ⠙ ⠋ ⠃ ⠅ ⠇ ⠏ ⠟ ⠗ ⠛ ⠓ ⠚ ⠢
```

### 2.5 Три режима копирования

| Режим | Описание | Telegram |
|-------|----------|----------|
| FLAT | Все эмоджи подряд, одна строка | ✓ работает, форма теряется |
| SHAPED | Строки с deco-отступами слева/справа | ✓ форма сохраняется |
| ARTISTIC | Deco внутри строк каждые 2 эмоджи | ✓ максимально красиво |

**Важно:** SHAPED и ARTISTIC используют deco-символы (не пробелы) для выравнивания.
Telegram и большинство мессенджеров не стрипуют unicode-символы с строк.

### 2.6 Декодирование — всё работает автоматически

Декодер при получении сообщения:
1. Парсит все grapheme clusters через `Intl.Segmenter`
2. Фильтрует: все deco-символы (ALL_DECO set), whitespace, newlines → пропускает
3. Проверяет anchor (первый и последний эмоджи — null с индексом формы)
4. Строит reverse map: `{emoji → char/bigram}`
5. Для каждого оставшегося эмоджи → char/bigram в rev map

Т.е. можно вставить FLAT / SHAPED / ARTISTIC — декодируется одинаково.

---

## 3. ДОПОЛНИТЕЛЬНЫЕ МЕХАНИЗМЫ

### 3.1 Anchor (якорь)
Если включён при кодировании: первый и последний эмоджи = `nullEmojis[shape_index]`.
Декодер автоматически определяет форму без подсказки, и убирает якоря перед декодом.

### 3.2 Null noise
Слайдер 0-40%: случайные null-эмоджи вставляются между символами.
Скрывает длину сообщения и структуру. Декодер игнорирует нулы автоматически.

### 3.3 Art mode
Сообщение утапливается в паддинге из декоративных эмоджи.
Снаружи выглядит как большой эмоджи-арт. Содержание скрыто внутри.

### 3.4 TTL токена
- `never` — вечный
- `N сообщений` — токен саморазрушается после N декодирований
- `30 дней` — истекает по дате

---

## 4. САЙТ — СТРАНИЦЫ И КОМПОНЕНТЫ

### Сплэш-страница
Терминал-аутентификация. Принимает: lain, drip, wired, шифр, няш, driptech, layer 07, anon, посвящённый, ghost, cipher, город, 07, lain, и ~15 вариантов.
Отказывает: привет, hello, hi, ok, да, yes, test.
Canvas starfield на фоне. Typewriter-лог загрузки.

### Tabs
- **LANGUAGES** — создание/импорт/управление языками, токен-дисплей
- **ENCODE** — кодировщик, выбор формы, 3 режима копирования
- **DECODE** — декодировщик, принимает все форматы
- **BOT_SIM** — симулятор Telegram inline режима
- **ALPHABET** — вся таблица маппинга активного языка

### Sidebar виджеты
- Часы (реальное время)
- Счётчик посетителей (LCD стиль, localStorage)
- Node status (encoded count, langs count)
- Music player (Web Audio API, синусоидальный дроун-эмбиент)
- Nyan Pet 🐱 (кликай, корми, 7 кликов → пасхалка)
- Mood selector (8 настроений, сохраняется)
- Guestbook (localStorage, 20 записей)
- Quote DB (11 цитат)
- Web Ring
- Under Construction strip
- Best viewed 1024×768 Netscape 4+

### Пасхалки
| Триггер | Пасхалка |
|---------|----------|
| ↑↑↓↓←→←→BA | Konami code |
| Напечатать "lain" | Serial Experiments Lain цитата |
| Напечатать "drip" | Дриптех приветствие |
| Напечатать "няш" | #няшныйдвиж |
| 7 кликов на лого | Layer 07 |
| Навести 5× на 👁 | Глаз |
| 7 кликов на кота | Nyan revelation |
| Напечатать "bigram" | Bigram engine easter egg |

---

## 5. ТЕХСТЕК

```
Язык:       HTML + CSS + Vanilla JS (ES2020+)
Зависимости: ТОЛЬКО Google Fonts (VT323, Share Tech Mono) — внешний CDN
Хранение:   localStorage (langs, активный язык, visits, encoded count, mood, guestbook)
Аудио:      Web Audio API (нативный браузер)
Clipboard:  navigator.clipboard.writeText + execCommand fallback
Emoji parse: Intl.Segmenter (grapheme clusters) + regex fallback
Размер:     ~85KB один файл
```

**Нет:** серверов, баз данных, фреймворков, npm, бандлеров.

---

## 6. ТЕКУЩИЕ БАГИ И СТАТУС

### Исправлено в последнем патче (v3.1)

| Баг | Статус | Что было | Что сделано |
|-----|--------|----------|-------------|
| Пробелы пропадают | ✅ | spaceEmoji = null если пул кончался | Гарантированный слот до nulls |
| ASCII в copy исчезают | ✅ | IDSP (U+3000) → Telegram стрипует | Заменили на deco-символы |
| Мало ASCII в ARTISTIC | ✅ | Только по краям строки | Теперь каждые 2 эмоджи внутри строки |
| "Х" в начале декода | ✅ | v2→v3 миграция ломала маппинг | Убрали миграцию, только v3 ключи |
| Мало биграмм | ✅ | 40 биграмм | 80 биграмм (тир-1 + тир-2) |

### Известные ограничения (не баги, архитектура)

| Проблема | Описание |
|----------|----------|
| ~1 эмоджи на букву | Биграммы дают ~22% сжатие. 80 символов = ~76 эмоджи. |
| Длинный текст | 200+ слов = 400+ эмоджи, в Telegram не отправится одним сообщением |
| Редкие слова | "ВИФЛЕЕМСКОЙ" — ни одна биграмма не срабатывает |
| # и спецсимволы | Пропускаются при кодировании |

---

## 7. ОБСУЖДАВШИЕСЯ ПУТИ РАЗВИТИЯ

### Для длинных текстов (отложено)
- **N-граммы + адаптивный словарь** — слова из реальных сообщений попадают в токен
- **Байтовый уровень** — gzip → 256-символьный emoji алфавит (1 эмоджи = 1 байт)
- **Чанкинг** — разбивка на части по 60-80 эмоджи с индексами [1/7]
- **Изображение** — canvas → PNG → отправляется как фото

### Для безопасности (отложено)
- Ротация токена `/rotatelang` — 30% ресаффл раз в неделю
- Иерархия токенов — мастер / читатель / групповой
- Публичный "Дриптех Стандарт" — один токен для всех подписчиков канала

---

## 8. КАК ПЕРЕНЕСТИ ПРОЕКТ

Весь проект = **один файл `emojirean.html`**.

Для деплоя достаточно:
1. Положить файл на любой хостинг (GitHub Pages, Netlify, Neocities, etc.)
2. Пользователь открывает в браузере → всё работает офлайн

Данные хранятся в localStorage браузера (привязаны к домену).
При смене домена — токены нужно экспортировать вручную и реимпортировать.

**Для интеграции с Telegram ботом (@drip_tech_f):**
- Сайт открывается через Telegram WebApp (`window.Telegram.WebApp`)
- Параметры передаются через `initData`
- Готовый интерфейс уже адаптирован к мобильному экрану

---

## 9. ФАЙЛЫ

```
emojirean.html       — основной файл, весь проект
emojirean_docs.md    — эта документация
```

---

*токен — это ключ · слово — это дверь · #няшныйдвиж*
